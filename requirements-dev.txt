# Development and test dependencies. Install alongside the runtime set:
#     pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0,<9.0
# fastapi.testclient needs an HTTP client. Starlette now prefers httpx2; plain
# httpx still works but emits a deprecation warning.
httpx2>=0.28
